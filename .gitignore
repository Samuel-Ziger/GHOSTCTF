node_modules/

# Payloads gerados pela UI (CTF) — mantém só .gitkeep
payloads/*
!payloads/.gitkeep

data/*.db
data/*.db-*
.env
.env.local

# Supabase CLI (ligação local ao projeto remoto)
supabase/.branches/
supabase/.temp/

# Artefactos locais (não enviar ao remoto)
/hash.txt
/key.pub
